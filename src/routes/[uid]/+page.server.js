import { createClient } from '$lib/prismicio'

// export const prerender = true;

export async function load({ params}) {
//   const client = createClient({ fetch, request });
// 
//   const document = await client.getByUID('horoscoperad', params.uid);
  
  return( params)
  };
 